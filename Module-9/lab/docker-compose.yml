services:
  app:
    build: ./app
    environment:
      - HOSTNAME=capacity-lab-01
      - ENVIRONMENT=training
    ports:
      - "5000:5000"
      - "8080:8080"
    restart: unless-stopped

volumes: {}
